import { Request, Response } from 'express';

class ShippingMethodsController {
  validateShippingMethod(req: Request, res: Response): void {
    throw new Error('Missing implementation');
  }
}
export default ShippingMethodsController;
